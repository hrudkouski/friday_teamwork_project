// Reducer
export const newPasswordReducer = (state: any, action: any): any => {
    switch (action.type) {
        default:
            return {...state}
    }
}

// Actions
// Types
// Initial State
// Action Creators
// Thunk Creators